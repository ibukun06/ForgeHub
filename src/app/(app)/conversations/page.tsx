import { PlaceholderScreen } from "@/components/app-shell/screens";

export default function ConversationsPage() {
  return (
    <PlaceholderScreen
      title="Conversations"
      description="Project-native messaging, threads, calls, and async updates that stay linked to real work artifacts."
    />
  );
}
