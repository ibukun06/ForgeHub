import { PlaceholderScreen } from "@/components/app-shell/screens";

export default function AdminPage() {
  return (
    <PlaceholderScreen
      title="Admin"
      description="Permissions, governance, policy, and automation controls that let ForgeHub scale from individuals to enterprise teams."
    />
  );
}
